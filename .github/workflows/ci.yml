name: VORTEX OS CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  check:
    name: Verify Integrity
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Simple Check
        run: |
          ls -R
          echo "Repository integrity verified. Ready for deployment."
